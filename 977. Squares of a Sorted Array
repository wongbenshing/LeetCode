class Solution(object):
    def sortedSquares(self, A):
        for i in range(len(A)):
            A[i] = A[i] ** 2
        return sorted(A)
        """
        :type A: List[int]
        :rtype: List[int]
        """
